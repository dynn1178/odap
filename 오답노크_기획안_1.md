# 오답노크 — 반복학습 문제풀이 서비스 기획안 (Claude Code 전달용)

## 0. 문서 목적
사용자가 작성한 요구사항(1~13번)을 검토·정리하고, 애매하거나 결정이 필요한 부분에 대해 구체적인 방안을 제안한 기획안입니다. 이 문서를 그대로 Claude Code(VS Code)에 전달해 웹앱 제작을 요청하는 용도로 사용할 수 있습니다.

---

## 1. 서비스 개요

- **이름(가칭)**: 오답노크
- **컨셉**: Anki식 간격반복(Spaced Repetition) 방식의 객관식 문제풀이 웹서비스
- **문제 형식**: 문장(단어/문장 구분 없음) + 보기 5개 중 1개 정답 선택
- **핵심 로직**: 틀리거나 어렵다고 표시한 문제는 더 자주, 잘 맞추는 문제는 점점 덜 등장
- **데이터 저장소**: Google Sheets (Google Sheets API)
- **배포**: Vercel (Next.js)
- **규모**: 문제 수 최대 2,000~3,000개, 여러 과목/시험 유형 지원, 다중 사용자

### 1-1. 네이밍 & 브랜딩 컨셉
"오답노크"는 오타가 아니라 **"오답을 노크(knock)한다"**는 의도된 언어유희입니다. 틀린 문제 앞에 계속 다시 찾아가서 "똑똑" 두드리듯 반복해서 마주친다는 컨셉을 담고 있으므로, 화면 곳곳에서 이 의미가 자연스럽게 드러나도록 시각적으로 표현하는 것을 제안합니다.

- **로고/파비콘**: 문 또는 문고리(door knocker) 모양 아이콘 + 말풍선/느낌표를 결합한 형태
- **오답 화면**: 정답을 틀렸을 때 "똑똑, 다시 만났네요" 같은 문구와 함께 **주먹으로 문을 두드리는 듯한 짧은 아이콘 애니메이션**(예: 노크 이모지 🚪✊ 또는 커스텀 SVG가 살짝 흔들리는 효과) 노출
- **오답 노출 빈도가 높은 문제 표시(대시보드 등)**: "자주 노크 중인 문제" 같은 라벨과 함께 노크 아이콘 배지 사용
- **로딩/전환 화면**: 문을 두드리는 짧은 마이크로 인터랙션으로 브랜드 톤 유지
- Claude Code에 전달 시에도 "오답노크는 오타가 아니라 '오답을 노크한다'는 언어유희이며, 이를 살린 아이콘/마이크로 인터랙션을 함께 구현해달라"는 문장을 프롬프트에 명시하는 것을 권장합니다(8번 요약 프롬프트에 반영함).

---

## 2. 요구사항 검토 및 확정/제안 사항

### 2-1. 보기 5개 & 정답 판정 방식 (원 요구사항 3, 4)
- 프론트에서 5개 보기(정답 1 + 오답 4)를 매 노출 시 `Fisher-Yates` 셔플로 순서를 무작위 배치
- 정답 판정은 **선택한 보기의 텍스트 값 === 정답 텍스트 값**으로 비교 (인덱스/번호 비교 금지)
- → 정답 텍스트가 우연히 오답 텍스트와 완전히 동일하면 오류가 나므로, 시트 입력 시 보기 5개는 서로 달라야 함(검증 필요)

### 2-2. "어려워" 체크와 오답 시 난이도 선택(실수/조금 어렵네/완전 어렵네) 관계 (원 요구사항 5, 12)
요구사항에 두 가지 난이도 입력이 섞여 있어 하나로 정리가 필요합니다. 다음 방식을 제안합니다.

| 상황 | UI | 가중치 부여 |
|---|---|---|
| 정답 | "다음 문제" 버튼만 노출 | 없음 (정답 처리 로직만 적용) |
| 오답 | 정답 표시 + **난이도 선택 3단계**(실수였어 / 조금 어렵네 / 완전 어렵네) 선택 후 다음 문제로 이동 | 선택한 난이도에 따라 가중치 차등 부여 |

→ 별도의 "어려워" 체크박스는 **오답 시의 3단계 난이도 선택으로 통합**하는 것을 제안합니다(원 요구사항 5의 "어려워"와 12의 3단계가 사실상 같은 기능이기 때문). 이견 없으면 이 구조로 진행하고, 만약 "정답을 맞췄어도 어렵다고 표시"하고 싶은 케이스가 실제로 필요하면 정답 화면에도 "어려웠지만 맞았어요" 체크를 추가하는 옵션을 별도로 드릴 수 있습니다.

### 2-3. 출제 빈도 알고리즘 (원 요구사항 6, 12) — 핵심 제안

**상태값 (문제별로 저장)**
- `score` (노출 가중치, 0부터 시작, 정수)
- `streak` (연속 정답 카운트, 0부터 시작)

**갱신 규칙**
```
[오답 시]
  실수였어      → score += 1
  조금 어렵네   → score += 2
  완전 어렵네   → score += 3
  streak = 0

[정답 시]
  streak += 1
  필요 연속정답수 = ceil(score / 2)   # score가 높을수록 더 여러 번 맞춰야 감소
  if streak >= 필요 연속정답수 and score > 0:
      score -= 1
      streak = 0
```
→ 이 방식이 "점수가 높을수록 한 번에 크게 안 깎이고, 누적된 만큼 여러 번 반복해서 맞춰야 낮아진다"는 요구사항 12를 정확히 구현합니다. (예: score=6이면 3번 연속 정답을 맞춰야 score가 5로 1 감소)

**노출 확률**
- 문제 풀이 화면에서 매번 전체 문제 중 하나를 뽑을 때, `가중치 = base(1) + score`로 하는 **가중치 기반 랜덤 추첨(weighted random)** 사용
- score=0인 문제도 base 가중치 1은 유지 → 완전히 사라지지 않고 최소한의 빈도로 계속 등장(복습 없는 문제가 없도록)
- 같은 문제가 바로 연속으로 다시 나오지 않도록 "최근 N개(예: 5개) 문제는 재출제 제외" 규칙 추가 권장

### 2-4. 사용자 식별 (원 요구사항 9)
- 이름 + 4자리 번호(PIN) 조합으로 사용자 식별, 비밀번호 찾기 없음
- **동명이인 + 동일 PIN 충돌 가능성**이 있으므로, 내부적으로는 `user_id = 이름_PIN`을 그대로 키로 쓰기보다 **최초 가입 시 (이름+PIN) 조합이 이미 존재하면 "이미 사용 중인 이름/번호입니다" 안내 후 재입력**을 받는 방식을 제안합니다. 별도의 로그인 이메일이나 인증은 요구사항에 없으므로 생략.

### 2-5. 대시보드 (원 요구사항 13)
요구사항 그대로 6개 항목 구성. "학습시간"은 클라이언트에서 문제 진입~응답 시각 차이를 누적해 세션 종료(또는 배치 flush) 시점에 초 단위로 합산 전송.

---

## 3. 데이터 구조 & API 호출 최적화 (원 요구사항 10, 11) — 가장 중요한 설계 포인트

### 3-1. 왜 문제가 되는가 (※ 외부 정보)
Google Sheets API는 **프로젝트 전체 분당 300건, 사용자(계정)당 분당 60건**의 읽기/쓰기 요청 제한이 있고, 셀 1개당 **최대 50,000자** 제한이 있습니다. 문제풀이 1건마다 행을 추가하거나 셀을 갱신하면, 사용자 수가 늘어날수록 순식간에 이 한도를 넘습니다. 또한 "문제 수 × 사용자 수"만큼 행이 생기는 구조는 시트가 수만~수십만 행까지 불어날 수 있어 관리도 어려워집니다.

### 3-2. 해결 방향 — 3가지를 결합

**① 사용자별 점수는 "행"이 아니라 "셀 하나의 JSON"으로 저장**
- 문제 1개당 1행이 아니라, **(사용자, 과목) 조합당 1행**만 만들고, 그 안의 한 셀에 해당 과목 전체 문제의 점수 상태를 압축 JSON으로 저장
- 예: `{"12":[2,1,5,2],"57":[0,0,3,0], ...}` → key=문제ID, value=[score, streak, 정답횟수, 오답횟수]
- 효과: 행 수가 "사용자 수 × 과목 수" 수준으로 유지됨 (문제 수·풀이 횟수와 무관)
- 주의: 문제 수가 아주 많고 사용자가 전 문제를 다 풀어본 경우 셀이 50,000자에 근접할 수 있음 → 과목별 문제 수가 1,000개를 넘으면 압축 표기(구분자 방식, 예: `"12:2,1,5,2|57:0,0,3,0"`)를 쓰거나, 필요 시 과목을 여러 시트로 분리하는 여유를 둡니다.

**② 클라이언트에서 답안을 모았다가 "배치"로 전송**
- 문제 하나 풀 때마다 API를 호출하지 않고, 브라우저 메모리(state)에 결과를 쌓아둠
- **10문제마다 또는 30~60초마다 또는 탭 종료/이탈 시점**에 한 번에 서버(Next.js API Route)로 전송
- 서버는 해당 사용자의 JSON 셀을 1회 읽고 → 메모리에서 갱신 계산 → 1회 쓰기로 반영 (배치당 API 호출 2회 수준)
- 오늘 문제풀이 수·정답률 등 대시보드용 요약 통계도 같은 배치 타이밍에 별도의 "일별 통계" 행에 함께 갱신

**③ 문제 은행(Questions) 데이터는 캐싱**
- 문제/정답은 자주 바뀌지 않으므로, Vercel의 서버리스 함수 또는 Next.js에서 **일정 시간(예: 10~30분) 캐싱**하여 매 요청마다 시트를 읽지 않도록 함
- 문제 데이터 변경 시에도 실시간 반영이 필수가 아니라면 캐시 TTL만으로 충분

이 3가지를 결합하면 사용자 수·문제풀이 횟수가 늘어나도 API 호출 수와 시트 행 수가 거의 늘지 않는 구조가 됩니다.

---

## 4. 구글시트 구성 (샘플 양식)

### 시트 ① `Questions` (문제/정답 — 과목 무관 하나로 통합, subject 컬럼으로 구분)
| 컬럼명 | 설명 |
|---|---|
| subject | 과목/시험유형 (예: 영어단어, 토익문법 등) |
| question_id | 문제 고유 ID |
| question_text | 문제(지문) |
| correct_answer | 정답 텍스트 |
| option_2 ~ option_5 | 오답 보기 4개 텍스트 |
| explanation | (선택) 해설 |
| active | 사용 여부 (Y/N) |

### 시트 ② `Users` (사용자)
| 컬럼명 | 설명 |
|---|---|
| user_id | 이름+PIN 기반 고유키 |
| name | 이름 |
| pin | 4자리 번호 |
| created_at | 가입일시 |

### 시트 ③ `UserProgress` (사용자별 점수 — 행 폭발 방지 핵심 시트)
| 컬럼명 | 설명 |
|---|---|
| user_id | 사용자 ID |
| subject | 과목 |
| progress_json | 문제ID별 [score, streak, 정답수, 오답수] 압축 JSON/문자열 |
| updated_at | 마지막 갱신 시각 |

### 시트 ④ `DailyStats` (일별 통계 — (사용자, 날짜)당 1행, upsert)
| 컬럼명 | 설명 |
|---|---|
| user_id | 사용자 ID |
| date | 날짜 |
| solved_count | 오늘 푼 문제 수 |
| correct_count | 정답 수 |
| wrong_count | 오답 수 |
| study_seconds | 학습 시간(초) |

### 시트 ⑤ `Comments` (한줄 남기기 — 소량이라 append 방식 그대로 사용 가능)
| 컬럼명 | 설명 |
|---|---|
| user_id | 작성자 |
| name | 이름 |
| comment | 내용 |
| created_at | 작성일시 |

---

## 5. 화면(페이지) 구성

1. **메인/시험 선택 화면** — 존재하는 시험 유형(subject) 목록 노출, 선택
2. **로그인 화면** — 이름 + 4자리 번호 입력 (신규면 자동 가입, 기존이면 조회)
3. **문제풀이 화면**
   - 문제 문장 + 보기 5개(랜덤 순서) 버튼
   - 정답: 정답 보기에 동그라미 표시 → "다음 문제" 버튼
   - 오답: 정답 보기 표시 + 3단계 난이도 선택 버튼 → 선택 후 다음 문제로
4. **대시보드 화면**
   - 오늘 성적(문제 수/정답률/맞음/틀림/학습시간)
   - 전체 누적 성적
   - 출석일자(캘린더 형태 권장)
   - 회원 랭킹(풀이 수, 정답률 기준)
   - 방문자 수
   - 한줄 남기기(목록 + 입력창)

---

## 6. 기술 스택 제안

- **프론트/백엔드**: Next.js (App Router) + Tailwind CSS, Vercel 배포
- **데이터 연동**: Google Sheets API (서비스 계정 인증) — Next.js API Route에서만 호출, 클라이언트는 직접 Sheets API를 호출하지 않음(키 노출 방지)
- **캐싱**: Next.js 서버 캐시 또는 in-memory 캐시로 Questions 데이터 TTL 캐싱
- **상태 관리**: 문제풀이 중 답안 큐는 클라이언트 메모리(React state) + 배치 전송

---

## 7. 개발 진행 확인이 필요한 사항 (Claude Code에 요청 전 결정 권장)

1. 2-2 항목: "어려워" 체크와 3단계 오답 난이도를 하나로 통합하는 안에 동의하는지
2. 문제 하나에 오답 보기가 항상 정확히 4개인지, 과목마다 보기 개수가 다를 수 있는지
3. 랭킹 산정 기준을 "전체 누적 풀이 수" 기준으로 할지 "최근 N일" 기준으로 할지
4. 서비스 계정 Google Sheets API 키 발급 및 시트 공유 설정을 준비했는지

---

## 8. Claude Code 전달용 요약 프롬프트 (그대로 복사해서 사용 가능)

> Next.js(App Router) + Tailwind CSS 기반으로 "오답노크"라는 간격반복 객관식 문제풀이 웹앱을 만들어줘. "오답노크"는 오타가 아니라 "오답을 노크(knock)한다"는 언어유희 네이밍이니, 문/노크를 모티브로 한 로고·아이콘·오답 시 마이크로 인터랙션을 함께 구현해줘. 데이터는 Google Sheets를 백엔드로 쓰고(서비스 계정 인증, Next.js API Route에서만 접근), Vercel에 배포할 거야. 시트 구조와 API 호출 최적화 전략, 화면 구성, 출제 빈도 알고리즘은 첨부한 기획안(오답노크_기획안.md)을 그대로 따라줘. 특히 3번(데이터 구조 & API 최적화) 섹션의 배치 전송·JSON 압축 저장 구조를 반드시 반영해줘.

