# 오답노크 — 반복학습 문제풀이 서비스 기획안 v2 (확정본)

> v1(`오답노크_기획안_1.md`)에 남겨주신 검토 코멘트를 모두 반영하고, 질문 주신 항목(시트 분리 기준 / 시트 구성 변경 / 한글 컬럼명 / 서비스 계정 필요값)에 답을 채운 문서입니다.
> **이 문서 하나만 Claude Code에 전달하면 됩니다.** v1은 히스토리 참고용으로만 남겨두세요.

## v1 → v2 변경 요약

| 항목 | v1 | v2 |
|---|---|---|
| 난이도 버튼 | 오답 시 3단계 고정 문구 | **정답 2종 + 오답 3종, 각 단계마다 문구 풀에서 랜덤 노출 + 가중치 배지(+1/−1) 표시** |
| 가중치 규칙 | 오답만 가중치 증가 | **"맞췄지만 불안" 정답도 +1**, score 상한 도입 |
| 출제 제외 | 권장 사항 | **최근 N개 제외 확정 + 미출제 문제 우선 노출 보너스** |
| 로그인 | 자동 가입 | **로그인 / 신규가입 분리, PIN 해시 저장, 랜덤 회원ID** |
| 대시보드 | 6개 항목 | **문제별 학습이력 장표 + 문제 상세 모달 추가** |
| 진도 저장 | `[score, streak, 정답수, 오답수]` | **버튼 이력 문자열 추가** (장표용) |
| 시트 | 영문 컬럼 5개 시트 | **한글 컬럼, 과목목록 시트 추가, 문제은행 과목별 분리** |
| UI | 미정 | **모바일 우선 반응형 + 라이트/다크/시스템 + 글자크기 4단계 + 폰트 3종** |
| 대시보드 위치 | 최상위 | **과목 하위 화면 — 선택한 과목 것만 보이고 다른 과목은 노출되지 않음** |

---

## 1. 서비스 개요

- **이름**: 오답노크
- **컨셉**: Anki식 간격반복(Spaced Repetition) 객관식 문제풀이 웹서비스
- **문제 형식**: 문장(단어/문장 구분 없음) + 보기 중 **정답 1개** 선택 (보기 총 개수는 과목/문제마다 가변, 3~7개)
- **핵심 로직**: 틀리거나 어렵다고 표시한 문제는 더 자주, 확실히 아는 문제는 점점 덜 등장
- **데이터 저장소**: Google Sheets (서비스 계정 + Sheets API)
- **배포**: Vercel (Next.js App Router)
- **규모**: 문제 2,000~3,000개, 다중 과목, 다중 사용자

### 1-1. 네이밍 & 브랜딩

"오답노크"는 오타가 아니라 **"오답을 노크(knock)한다"** 는 의도된 언어유희입니다. 틀린 문제 앞에 계속 다시 찾아가 "똑똑" 두드리듯 마주친다는 컨셉이므로, 화면 곳곳에서 이 의미가 드러나야 합니다.

- **로고/파비콘**: 문 + 문고리(door knocker) 아이콘에 말풍선/느낌표 결합 (인라인 SVG, 라이트/다크 양쪽 대응)
- **오답 화면**: "똑똑, 다시 만났네요" 문구 + 주먹이 문을 두드리는 짧은 흔들림 애니메이션(200~400ms, `prefers-reduced-motion` 존중)
- **자주 틀리는 문제 배지**: 대시보드에서 score 상위 문제에 **"자주 노크 중"** 라벨 + 노크 아이콘
- **로딩/전환**: 노크 마이크로 인터랙션으로 브랜드 톤 유지

---

## 2. 핵심 로직 (확정)

### 2-1. 보기 셔플 & 정답 판정

- 보기 배열 = `[정답] + [오답들]` 을 매 노출 시 **Fisher-Yates 셔플**
- 정답 판정은 **선택한 보기의 텍스트 === 정답 텍스트** 비교 (인덱스/번호 비교 금지)
- 보기 개수는 문제마다 다를 수 있음 → 시트에서 빈 보기 칸은 자동 제외
- **시트 데이터 검증**(앱 기동/캐시 갱신 시 1회 수행, 콘솔 경고 + 관리 화면 표시):
  - 한 문제 안에 동일한 보기 텍스트가 2개 이상 → 경고 후 중복 제거
  - 보기(정답 포함)가 2개 미만 → 해당 문제 출제 제외
  - 정답 칸이 비어 있음 → 출제 제외

### 2-2. 응답 버튼 = 대화형 문구 + 가중치 배지 (확정)

정답/오답 모두에서 **버튼 선택으로 가중치를 조절**합니다. 단계별로 문구 풀(pool)을 두고 **매번 랜덤으로 하나를 뽑아** 노출해, 대화하는 느낌을 줍니다. 각 버튼 하단에는 가중치 변화를 **작은 배지**로 표시합니다.

> 구현 주의: 문구는 **문제를 채점한 시점에 한 번만 뽑아 고정**합니다(리렌더마다 바뀌면 눈이 어지럽습니다). 직전에 나온 문구는 연속으로 다시 뽑지 않습니다.

#### ✅ 정답을 맞췄을 때 (2단계)

| 단계 | 의미 | 문구 풀 (랜덤 노출) | 배지 |
|---|---|---|---|
| **A. 확실히 앎** | 진짜 아는 문제 → 노출 빈도 감소 | 다음 문제 / 쉽네 / 고고!! / 좋았어! / 이건 알지 / 껌이지 / 바로 다음 / 자신 있음 / 눈 감고도 맞춤 / 이 정도야 뭐 | `−1 · 연속 2/3` |
| **B. 맞췄지만 불안** | 찍었거나 헷갈림 → 노출 빈도 증가 | 사실 찍었음 / 완전 우연임 / 이게 맞다고? / 맞췄지만 어렵네 / 운이 좋았다 / 얼떨결에 맞음 / 감으로 골랐어 / 아직 잘 모르겠어 / 반은 찍은 듯 / 다시 보고 싶어 | `+1` |

#### ❌ 틀렸을 때 (3단계)

| 단계 | 의미 | 문구 풀 (랜덤 노출) | 배지 |
|---|---|---|---|
| **1. 실수** | 알았는데 놓침 | 아깝게 틀림 / 맞출 수 있었는데 / 실수였어 / 알고 있었는데! / 손이 미끄러졌어 / 아 이거였지 / 착각했다 / 거의 맞췄는데 | `+1` |
| **2. 조금 어렵네** | 애매하게 앎 | 좀 어려웠다 / 기억날 듯 말 듯 / 몇 번 더 보면 알겠지 / 가물가물해 / 헷갈리는 문제네 / 반쯤은 알겠어 / 아슬아슬했다 | `+2` |
| **3. 완전 어렵네** | 전혀 모름 | 처음 보는건데? / 너무 어렵다 / 난이도 극상 / 세게 노크해줘 / 하나도 모르겠어 / 이건 새로 외워야 해 / 백지야 백지 | `+3` |

- 정답/오답 화면 모두 **정답 보기를 시각적으로 강조**하고, 해설이 있으면 함께 노출
- 버튼은 모바일에서 세로 스택, 태블릿/PC에서 가로 배치 (최소 터치 영역 44×44px)
- 배지는 `text-xs opacity-60` 수준으로 작게 — 정보는 주되 시선을 뺏지 않게

### 2-3. 출제 빈도 알고리즘 (v2 갱신 규칙)

**문제별 상태값**

| 값 | 설명 |
|---|---|
| `score` | 노출 가중치. 0부터 시작, **0 ~ 10으로 클램프** |
| `streak` | 연속 "확실히 앎" 정답 카운트 |
| `correct` / `wrong` | 누적 정답 / 오답 횟수 (통계·장표용) |
| `history` | 최근 12회 응답 이력 문자열 (장표용, 3-3 참조) |

**갱신 규칙 (의사코드)**

```
MAX_SCORE = 10

# --- 정답 A: 확실히 앎 ---
streak += 1
correct += 1
필요_연속정답수 = max(1, ceil(score / 2))
if score > 0 and streak >= 필요_연속정답수:
    score -= 1
    streak = 0
if score == 0:
    streak = 0            # 이미 0이면 streak 누적은 의미 없음

# --- 정답 B: 맞췄지만 불안 ---
correct += 1
score = min(MAX_SCORE, score + 1)
streak = 0

# --- 오답 1: 실수       --- wrong += 1; score = min(MAX_SCORE, score + 1); streak = 0
# --- 오답 2: 조금 어렵네 --- wrong += 1; score = min(MAX_SCORE, score + 2); streak = 0
# --- 오답 3: 완전 어렵네 --- wrong += 1; score = min(MAX_SCORE, score + 3); streak = 0
```

- `필요_연속정답수 = ceil(score/2)` → **점수가 높을수록 여러 번 연속으로 맞춰야 1 감소**
  (score=6이면 3연속 정답 → score 5, score=2면 1번만 맞춰도 → score 1)
- 배지의 `연속 2/3` 표기는 `streak+1 / 필요_연속정답수` 를 그대로 보여주는 것
- **MAX_SCORE=10 상한을 둔 이유**: 상한이 없으면 특정 한 문제가 가중치를 독점해 다른 문제가 거의 안 나옵니다. 상한 10이면 최대 노출 배율이 11배로 충분히 강하면서도 다른 문제가 계속 순환합니다.

**노출 확률 (가중치 랜덤 추첨)**

```
가중치(문제) =
    3           # 아직 한 번도 안 풀어본 문제 (신규 우선 노출 보너스)
    1 + score   # 풀어본 적 있는 문제
```

- score=0이어도 base 1은 유지 → 완전히 사라지지 않고 최소 빈도로 계속 복습됨
- 미출제 문제에 3을 주는 이유: 그냥 1이면 새 문제가 묻혀서 진도가 안 나갑니다. 3이면 신규가 먼저 소진되면서도 기존 오답이 계속 섞여 나옵니다.

**최근 문제 재출제 제외 (확정)**

```
제외개수 N = min(5, 출제후보수 - 1)        # 후보가 적으면 자동으로 줄어듦
후보 = 전체출제가능문제 - 최근_출제_N개
if 후보가 비면: 후보 = 전체출제가능문제     # 안전장치
```

- 최근 출제 목록은 클라이언트 메모리(ring buffer) + `sessionStorage`에 보관 (시트 저장 불필요)
- 과목을 바꾸면 초기화

### 2-4. 사용자 식별 (확정)

**로그인과 신규가입을 화면에서 분리**합니다. (자동 가입 방식은 "중복이면 재입력" 요구와 양립할 수 없습니다 — 기존 사용자의 로그인인지 이름 충돌인지 구분할 방법이 없기 때문입니다.)

| 화면 | 동작 |
|---|---|
| **로그인** | 이름 + PIN 입력 → 일치하는 회원이 없으면 *"이름 또는 번호가 맞지 않아요"* |
| **처음이에요 (가입)** | 이름 + PIN 입력 → **(이름+PIN) 조합이 이미 있으면 "이미 사용 중인 이름/번호입니다. 다른 번호로 다시 입력해 주세요"** 후 재입력 |

- **회원ID는 `이름_PIN`이 아니라 랜덤 문자열**(`u_` + 10자리)로 발급 → 이름 변경·동명이인 대응이 자유롭고, 시트 곳곳에 이름/PIN이 흩어지지 않습니다.
- **PIN은 평문 저장 금지, 해시 저장**(`sha256(회원별_솔트 + PIN)`). 4자리라 암호학적 강도는 낮지만, 최소한 시트를 열람하는 사람이 남의 PIN을 그대로 보는 상황은 막습니다.
- 세션은 서명된 HttpOnly 쿠키 — 비밀번호 찾기/이메일 인증은 요구사항에 없으므로 없음
- 같은 이름으로 로그인 5회 실패 시 60초 대기 (무차별 대입 최소 방어)

### 2-5. 대시보드 (v2 확정)

> **대시보드는 최상위가 아니라 과목 하위 화면입니다.**
> 과목을 고른 뒤 [문제풀이 | 대시보드] 탭으로 오가며, 아래 모든 수치는 **그 과목 기준으로만** 집계됩니다.
> 이 화면에서 다른 과목은 일절 노출되지 않습니다. 여러 과목이 함께 보이는 곳은 첫 화면(과목 선택)뿐입니다.

**기본 6종**
1. 오늘 성적 — 푼 문제 수 / 정답률 / 맞음 / 틀림 / 학습시간
2. 전체 누적 성적 — 동일 지표의 누적치
3. 출석일자 — 월 단위 캘린더 히트맵 (이 과목을 학습한 날 = 노크 아이콘 표시)
4. 회원 랭킹 — **이 과목의 누적 풀이 수 기준**(확정), 동점 시 정답률 → 가입일 순
5. 방문자 수 — 오늘 이 과목을 공부한 사람 수 / 이 과목 누적 학습자 수
6. 한줄 남기기 — 이 과목에 남긴 글 목록(최신 20개) + 입력창

**7. 문제별 학습이력 장표 (신규)**

과목별로 내가 풀어본 문제를 표로 나열합니다.

| 컬럼 | 내용 |
|---|---|
| 문제ID | 시트의 문제 고유 ID |
| 문제 요약 | 앞 30자 + `…` |
| 총 풀이 | 정답+오답 횟수 |
| 맞음 / 틀림 | 각 횟수 |
| 정답률 | % |
| 현재 점수 | `score` (0~10) + 노출 배율 (예: `7 · 8배`) |
| 응답 경과 | 최근 12회 이력을 **아이콘 타임라인**으로 표시 (왼쪽=과거, 오른쪽=최근) |
| 상태 배지 | score ≥ 7 → **"자주 노크 중"** / score = 0 이고 최근 3회 연속 정답 → "정복!" |

- **응답 경과 아이콘 범례**: `●` 확실히 앎(초록) · `◐` 맞췄지만 불안(노랑) · `1` 실수(주황) · `2` 조금 어렵네(빨강) · `3` 완전 어렵네(진빨강)
- 정렬: 현재 점수 ↓ / 총 풀이 ↓ / 정답률 ↑ / 문제ID 토글, 검색창(문제 텍스트) 제공
- **행을 누르면 문제 상세 모달**: 문제 전문 + 전체 보기(정답 강조) + 해설 + 이 문제의 전체 이력 타임라인 + "이 문제 다시 풀기" 버튼
  - 모바일에서는 바텀시트, PC에서는 중앙 모달
- 데이터 출처: **진도 시트의 압축 문자열 + 이미 캐시된 문제은행** → 추가 시트나 추가 API 호출 없음

**학습시간 측정**: 문제 노출 시각 ~ 응답 시각의 차이를 누적. 한 문제당 **최대 120초로 캡**(자리 비움 방지), 탭이 백그라운드로 가면(`visibilitychange`) 타이머 정지. 배치 전송 시 초 단위로 합산 전송.

---

## 3. 데이터 구조 & API 호출 최적화

### 3-1. 제약 (Google Sheets)

- 요청 한도: **프로젝트 분당 300건 / 사용자당 분당 60건** 수준
- **셀 1개당 최대 50,000자**
- 문서 1개당 총 셀 수 상한 약 1,000만 셀

문제풀이 1건마다 행을 추가/갱신하면 사용자 수가 늘수록 순식간에 한도를 넘고, "문제 수 × 사용자 수"만큼 행이 생기면 수만~수십만 행으로 불어나 관리도 불가능합니다.

### 3-2. 해결책 3종 (세부 확정)

**① 사용자별 점수는 "행"이 아니라 "셀 하나의 압축 문자열"**

- (사용자 × 과목)당 **1행**, 그 안의 `진도데이터` 셀 하나에 해당 과목 전체 상태를 저장
- 행 수 = 사용자 수 × 과목 수 (문제 수·풀이 횟수와 무관)

**② 클라이언트에서 모았다가 배치 전송**

- 문제를 풀 때마다 API 호출 ❌ → 결과를 React state 큐에 쌓아둠
- **전송 트리거**: 10문제마다 / 60초마다 / 과목 이탈·대시보드 이동 시 / 탭 종료 시(`visibilitychange` + `navigator.sendBeacon`)
- 서버(Next.js Route Handler)는 **읽기 1회 → 메모리 병합 → 쓰기 1회** (배치당 API 2~3콜)
- **중요 — 병합 방식**: 클라이언트는 *계산된 최종 score*가 아니라 **"어떤 문제에 어떤 버튼을 눌렀다"는 이벤트 목록**을 보냅니다. 서버가 시트의 현재 상태 위에 이벤트를 순서대로 적용하므로, 두 탭에서 동시에 풀어도 결과가 덮어써지지 않고 합쳐집니다.
- 전송 실패 시 큐를 `localStorage`에 보관 후 재시도 (최대 3회, 지수 백오프)

**③ 문제은행은 캐싱**

- 서버 in-memory 캐시 + `revalidate` **TTL 15분**
- 관리자가 문제를 고친 직후 반영이 필요하면 `/api/cache/purge?key=…` 수동 무효화 엔드포인트 제공
- 클라이언트는 과목 진입 시 해당 과목 문제 전체를 한 번 받아 메모리에 보관 → 문제를 넘길 때 네트워크 호출 0회

**예상 호출량**: 사용자 20명이 동시에 10분간 학습 → 20명 × 배치 10회 × 3콜 ≈ 600콜 / 10분 = **분당 60콜** → 한도 내. (문제은행은 캐시라 사실상 0)

### 3-3. 진도 데이터 압축 포맷 (확정)

문제별 레코드를 `|` 로 이어 붙인 **단일 문자열**로 저장합니다. JSON보다 30~40% 짧아 셀 한도에 여유가 생기고, 시트에서 눈으로 읽기도 그럭저럭 가능합니다.

```
문제ID:score,streak,정답수,오답수,최근이력
```

예시:

```
12:3,1,5,2,SS1L2|57:0,0,3,0,SSS|103:7,0,1,4,3321
```

- `최근이력` 문자 코드 (**왼쪽=과거, 오른쪽=최근, 최대 12자**)

  | 문자 | 의미 |
  |---|---|
  | `S` | 정답 · 확실히 앎 |
  | `L` | 정답 · 맞췄지만 불안 |
  | `1` | 오답 · 실수 |
  | `2` | 오답 · 조금 어렵네 |
  | `3` | 오답 · 완전 어렵네 |

- **한 번도 안 푼 문제는 아예 기록하지 않음** (미출제 = 키 없음)
- 레코드 1개 ≈ 평균 26자 → **문제 1,000개 = 약 26,000자** (한도 50,000자의 52%)

### 3-4. 시트 분리 기준 (질문하신 항목 — 확정 제안)

**결론: 분리 기준은 "과목"이고, 과목당 문제 수 상한은 1,000개입니다.**

| 시트 | 분리 여부 | 기준 |
|---|---|---|
| **문제은행** | **과목마다 1시트로 분리** | 관리 편의(사람이 직접 입력·수정) + 캐시 단위가 과목이라 초기 로딩이 빨라짐 |
| **진도** | **분리하지 않음 (통합 1시트)** | 행 수 = 사용자 × 과목이라 애초에 안 불어남. 사용자 1,000명 × 과목 10개여도 1만 행 |
| 회원 / 일별통계 / 한줄남기기 / 과목목록 | 분리하지 않음 | 소량 |

**과목이 커졌을 때의 분할 규칙**

1. **트리거**: 한 과목의 활성 문제가 **800개를 넘으면 분할 준비, 1,000개를 넘으면 반드시 분할**
   (1,000개 ≈ 진도 셀 26,000자 = 한도의 절반. 여기서 멈추면 안전 마진 2배 확보)
2. **분할 기준 (우선순위 순)**
   - ① **의미 단위 우선** — 챕터/파트/출제범위/난이도처럼 사용자가 "골라서 공부하고 싶은" 축으로 자릅니다.
     예: `토익문법` → `토익문법_품사` / `토익문법_시제` / `토익문법_관계사`
   - ② 의미 축이 없으면 **일련번호 구간**으로 기계적 분할.
     예: `영어단어_1(1~800)` / `영어단어_2(801~1600)`
   - ③ **한 번 자른 경계는 다시 바꾸지 않기** — 문제를 다른 과목으로 옮기면 그 사용자의 해당 문제 진도가 끊깁니다. 새 문제는 항상 **마지막 과목에 추가**하고, 그것도 800개가 차면 새 과목을 만듭니다.
3. **분할 후에는 앱이 이를 그냥 별개 과목으로 취급**합니다 → 진도 셀도 자동으로 나뉘고, 메인 화면 과목 목록에 나란히 노출됩니다.
4. **문제ID는 과목 경계와 무관하게 문서 전체에서 유일**하게 유지 (예: `ENG-0001`) — 나중에 과목을 재편해도 이력을 살릴 여지가 남습니다.
5. `과목목록` 시트에 `상위그룹` 컬럼을 두어, 쪼개진 과목들을 메인 화면에서 하나로 묶어 보여줍니다 (사용자에겐 "영어단어 > 1편/2편"처럼 보임).

---

## 4. 구글시트 구성 (한글 컬럼 — 확정)

### 4-0. 한글 컬럼명, 가능합니다

앱은 **1행(헤더)의 텍스트로 열을 찾습니다**(열 순서 하드코딩 ❌). 코드에는 `헤더명 → 내부 필드` 매핑 상수 파일(`src/lib/sheets/schema.ts`) 하나만 두므로, 나중에 헤더명을 바꾸고 싶으면 그 파일 한 곳만 고치면 됩니다.

지켜야 할 규칙:
- 헤더는 **반드시 1행**, 셀 앞뒤 공백·줄바꿈 금지 (앱에서 `trim()` 처리는 하지만 눈으로도 깔끔하게)
- 헤더명 중복 금지
- **열 순서는 자유롭게 바꿔도 됨**, 열을 새로 추가해도 앱은 무시하므로 메모용 열 추가 자유
- 시트 이름은 `과목목록`에 등록된 값을 그대로 사용

### 4-1. 시트 `과목목록` (신규 — 앱이 어떤 과목/시트를 읽을지 결정하는 진입점)

| 컬럼명 | 설명 | 예시 |
|---|---|---|
| 과목코드 | 영문/숫자 고유 코드 (**변경 금지**) | `ENG_WORD_1` |
| 과목명 | 화면에 보이는 이름 | `영어단어 1편` |
| 상위그룹 | (선택) 묶어서 보여줄 그룹명 | `영어단어` |
| 문제시트명 | 이 과목의 문제가 든 시트 이름 | `문제_영어단어1` |
| 설명 | (선택) 과목 카드에 노출할 한 줄 설명 | `수능 필수 800단어` |
| 노출순서 | 숫자 오름차순 정렬 | `1` |
| 사용여부 | `Y` / `N` | `Y` |

### 4-2. 시트 `문제_○○○` (과목마다 1개)

| 컬럼명 | 설명 |
|---|---|
| 문제ID | 문서 전체에서 유일한 값 (예: `ENG-0001`) |
| 문제 | 문제 지문/문장 |
| 정답 | 정답 텍스트 (**항상 1개**) |
| 보기2 ~ 보기7 | 오답 보기 (**빈칸 허용** — 채워진 것만 사용, 최소 1개 이상) |
| 해설 | (선택) 해설 텍스트 |
| 사용여부 | `Y` / `N` (N이면 출제 제외) |

> 보기 개수가 문제마다 달라도 됩니다. `보기2`만 채우면 2지선다, `보기7`까지 채우면 7지선다로 자동 동작합니다.

### 4-3. 시트 `회원`

| 컬럼명 | 설명 |
|---|---|
| 회원ID | `u_` + 랜덤 10자 (앱이 생성, 손대지 말 것) |
| 이름 | 사용자 입력 이름 |
| PIN해시 | `sha256(솔트 + PIN)` — 평문 저장 안 함 |
| 솔트 | 회원별 랜덤 문자열 |
| 가입일시 | ISO 8601 |
| 최근접속일시 | ISO 8601 |

### 4-4. 시트 `진도` (행 폭발 방지 핵심)

| 컬럼명 | 설명 |
|---|---|
| 회원ID | 회원 시트의 회원ID |
| 과목코드 | 과목목록의 과목코드 |
| 진도데이터 | 3-3의 압축 문자열 |
| 갱신일시 | ISO 8601 |

### 4-5. 시트 `일별통계` ((회원, 날짜, 과목)당 1행, upsert)

대시보드가 과목 하위 화면이므로 **과목코드까지 키에 포함**합니다.
행 수는 여전히 "회원 × 학습한 날 × 과목" 수준이라 부담이 없습니다.

| 컬럼명 | 설명 |
|---|---|
| 회원ID | |
| 날짜 | `YYYY-MM-DD` (Asia/Seoul 기준) |
| 과목코드 | 과목목록의 과목코드 |
| 푼문제수 | |
| 정답수 | |
| 오답수 | |
| 학습시간초 | |

> **출석일자·방문자 수·랭킹은 모두 이 시트에서 계산**합니다. 별도 시트나 추가 쓰기가 필요 없습니다.
> 모든 계산은 **먼저 과목코드로 걸러낸 뒤** 수행합니다.
> - 출석 = 그 과목에 해당 회원의 그 날짜 행이 있는지
> - 오늘 방문자 수 = 그 과목의 오늘 날짜 행에 등장하는 회원 수
> - 랭킹 = 그 과목의 회원별 `푼문제수` 합계 (누적 기준, 확정)

### 4-6. 시트 `한줄남기기`

| 컬럼명 | 설명 |
|---|---|
| 작성일시 | ISO 8601 |
| 회원ID | |
| 과목코드 | 어느 과목에 남긴 글인지 |
| 이름 | 작성 시점 이름 스냅샷 |
| 내용 | 최대 200자 |

> 소량이라 `append` 방식 그대로 사용. 목록은 최신 20개만 읽고 TTL 60초 캐시.

### 4-7. 시트 구성 총정리

```
스프레드시트 1개
├─ 과목목록          ← 앱이 가장 먼저 읽는 진입점
├─ 문제_영어단어1     ← 과목마다 1시트 (최대 1,000행)
├─ 문제_영어단어2
├─ 문제_토익문법
├─ 회원
├─ 진도              ← 사용자 × 과목당 1행
├─ 일별통계
└─ 한줄남기기
```

---

## 5. 화면 구성

```
/                        과목 선택  ← 여러 과목이 보이는 유일한 화면
└─ /study?subject=       문제풀이   ┐ 과목 안으로 들어오면
└─ /dashboard?subject=   대시보드   ┘ 이 과목 것만 보입니다
/settings                테마 · 글자 크기 · 폰트
/login                   로그인 / 신규가입
```

1. **메인 / 과목 선택** — `과목목록` 기준 카드 목록(상위그룹으로 묶음), 각 카드에 내 진행률(푼 문제/전체) 표시
2. **로그인 / 신규가입** — 탭 2개로 분리 (2-4 참조)
3. **문제풀이** — 상단에 과목명과 [문제풀이 | 대시보드] 탭
   - 상단: 과목명, 나가기, 이번 세션 진행 수
   - 본문: 문제 지문 + 보기 버튼(셔플)
   - 채점 후: 정답 강조 + 해설 + **응답 버튼(2-2)** + 오답 시 노크 애니메이션
   - 하단 고정 액션 바(모바일)
4. **대시보드(과목 하위)** — 2-5의 7개 섹션. 선택한 과목 기준으로만 집계되며 다른 과목은 보이지 않음
5. **문제 상세 모달/바텀시트** — 대시보드 장표에서 진입
6. **설정** — 테마(라이트/다크/시스템), 글자 크기, 폰트, 로그아웃

---

## 6. UI/UX 요구사항 (신규 확정)

### 6-1. 다기종 대응 — 모바일 우선

- **모바일 우선 설계**, 태블릿·PC는 확장 레이아웃
- 브레이크포인트: `~640px` 모바일 / `641~1024px` 태블릿(2단) / `1025px~` PC(최대폭 1,100px 중앙 정렬)
- 터치 타깃 최소 **44×44px**, 주요 액션은 **엄지 도달 범위(화면 하단)** 에 배치
- iOS safe-area 대응(`env(safe-area-inset-bottom)`), 입력 시 화면 확대 방지를 위해 input `font-size ≥ 16px`
- 가로/세로 회전 모두 대응, 긴 보기 텍스트는 줄바꿈 허용(**말줄임 금지** — 문제 내용이 잘리면 안 됨)

### 6-2. 라이트 / 다크 모드

- **라이트 / 다크 / 시스템 따름** 3택, 선택값 `localStorage` 저장
- Tailwind `darkMode: "class"` + CSS 변수로 색 토큰 정의 → 컴포넌트에서 색을 직접 쓰지 않음
- 첫 페인트 전 `<head>` 인라인 스크립트로 클래스 적용 (다크 모드에서 흰 화면 번쩍임 방지)
- 정답 초록 / 오답 빨강은 **다크 모드에서 채도를 낮춘 별도 토큰** 사용, 색맹 대비를 위해 **색 + 아이콘 병행 표기**

### 6-3. 글자 크기 · 폰트 선택

| 설정 | 옵션 |
|---|---|
| 글자 크기 | 작게(15px) / 보통(17px) / 크게(19px) / 아주 크게(21px) — `html { font-size }` 조절, 전 화면 `rem` 단위 사용 |
| 폰트 | 고딕(Pretendard) / 명조(세리프) / 시스템 기본 |

- 설정 화면에 **미리보기 문장** 제공, 값은 `localStorage` 저장 후 즉시 적용
- 웹폰트는 `font-display: swap` + 한글 서브셋, "시스템 기본" 선택 시 웹폰트 미로딩(저사양 기기 배려)
- `prefers-reduced-motion` 이면 노크 애니메이션 비활성

---

## 7. 기술 스택

- **프레임워크**: Next.js (App Router, TypeScript) + Tailwind CSS
- **배포**: Vercel
- **데이터**: Google Sheets API v4 (서비스 계정) — **Route Handler에서만 호출**, 클라이언트는 절대 직접 호출하지 않음(키 노출 방지)
- **캐싱**: 서버 in-memory + `unstable_cache`/`revalidate` (문제은행 TTL 15분)
- **상태**: 풀이 큐는 React state + `localStorage` 백업 → 배치 전송
- **라이브러리 최소화**: `googleapis`(또는 `google-auth-library` + fetch). 별도 상태관리 라이브러리 불필요

---

## 8. 서비스 계정 준비 — 필요한 값 (질문하신 항목)

### 8-1. Google Cloud 콘솔에서

1. 프로젝트 선택 → **API 및 서비스 > 라이브러리 > "Google Sheets API" 사용 설정**
2. **사용자 인증 정보 > 서비스 계정 만들기** → 만든 계정의 **키 > 키 추가 > JSON** 다운로드
3. 받은 JSON에서 아래 **2개 값**만 쓰면 됩니다:
   - `client_email` — 예: `odapknock@내프로젝트.iam.gserviceaccount.com`
   - `private_key` — `-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n`

### 8-2. 스프레드시트 쪽에서

4. 스프레드시트 우측 상단 **공유** → 위 `client_email` 주소를 **편집자(Editor)** 로 추가
   (이걸 빼먹으면 인증은 되는데 `The caller does not have permission` 오류가 납니다 — 가장 흔한 실수)
5. 스프레드시트 URL에서 **ID** 추출
   `https://docs.google.com/spreadsheets/d/` **`1AbCdEf...XyZ`** `/edit` → 굵은 부분

### 8-3. 환경변수 (`.env.local` 및 Vercel > Settings > Environment Variables)

```env
GOOGLE_SERVICE_ACCOUNT_EMAIL=odapknock@내프로젝트.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n"
GOOGLE_SHEET_ID=1AbCdEf...XyZ
SESSION_SECRET=아무_긴_랜덤_문자열_32자_이상
PIN_SALT_SECRET=아무_긴_랜덤_문자열_32자_이상
```

**주의사항**
- `GOOGLE_PRIVATE_KEY`는 줄바꿈이 `\n` 문자로 들어가므로 **큰따옴표로 감싸고**, 코드에서 `.replace(/\\n/g, "\n")` 처리합니다.
- Vercel에 넣을 때는 JSON의 `private_key` 값을 **그대로 붙여넣기**하면 됩니다(Vercel은 실제 줄바꿈도 허용).
- **JSON 키 파일 자체를 저장소에 커밋하지 마세요.** `.gitignore`에 키 파일과 `.env*` 를 추가합니다.
- 서비스 계정 권한은 **해당 스프레드시트 1개에만** 부여하면 충분합니다(드라이브 전체 권한 불필요).

---

## 9. v1 질문에 대한 답 정리

| # | 질문 | 결정 |
|---|---|---|
| 1 | "어려워" 체크와 3단계 난이도 통합? | **통합 후 확장** — 정답 2단계 + 오답 3단계, 대화형 랜덤 문구 (2-2) |
| 2 | 보기 개수는 항상 5개? | **가변(3~7개), 정답은 항상 1개** — 시트 빈칸 허용 (4-2) |
| 3 | 랭킹 기준? | **전체 누적 풀이 수** (동점 시 정답률 → 가입일) |
| 4 | 서비스 계정 필요값? | **8번 섹션 참조** (client_email / private_key / 스프레드시트 ID + 편집자 공유) |
| 5 | 시트 구성 변경 필요? | **변경됨** — 한글 컬럼, `과목목록` 시트 신설, 문제은행 과목별 분리, 진도데이터 포맷 확장 (4번) |
| 6 | 시트 분리 기준? | **과목 단위, 과목당 1,000문제 상한** (3-4) |
| 7 | 한글 컬럼명 가능? | **가능** — 헤더명 기반 매핑 (4-0) |
| 8 | 대시보드 위치? | **과목 하위 화면** — 모든 수치가 그 과목 기준, 다른 과목 비노출 (2-5) |

---

## 10. Claude Code 전달용 요약 프롬프트

> Next.js(App Router, TypeScript) + Tailwind CSS로 **"오답노크"** 간격반복 객관식 문제풀이 웹앱을 만들어줘. 첨부한 기획안(`오답노크_기획안_2.md`)을 그대로 따라줘.
>
> - "오답노크"는 오타가 아니라 **"오답을 노크(knock)한다"** 는 언어유희 네이밍이야. 문/노크를 모티브로 한 로고·아이콘·오답 시 마이크로 인터랙션을 반드시 함께 구현해줘.
> - 데이터는 Google Sheets 백엔드(서비스 계정 인증). **Sheets API는 Next.js Route Handler에서만** 호출하고 클라이언트에서는 절대 직접 호출하지 마.
> - **3번 섹션(데이터 구조 & API 최적화)의 배치 전송 + 압축 문자열 저장 구조는 반드시 그대로 반영**해줘. 특히 클라이언트는 계산된 점수가 아니라 **응답 이벤트 목록**을 보내고, 서버가 시트의 현재 상태 위에 이벤트를 적용해 병합하는 방식이어야 해.
> - **2-2의 대화형 랜덤 버튼 문구 풀과 가중치 배지**, **2-3의 갱신 규칙 · 가중치 추첨 · 최근 N개 제외**를 정확히 구현해줘.
> - **대시보드는 최상위가 아니라 과목 하위 화면**이야. 과목을 고른 뒤 [문제풀이 | 대시보드] 탭으로 오가고, 대시보드의 모든 수치(오늘/누적/출석/랭킹/방문자/한줄남기기/문제별 이력)는 그 과목 것만 보여야 해. 다른 과목이 보이면 안 돼.
> - **2-5의 문제별 학습이력 장표와 문제 상세 모달**을 대시보드에 포함해줘.
> - 시트 컬럼명은 **한글**이고, 앱은 열 순서가 아니라 **헤더명으로 열을 찾아야** 해. 헤더↔필드 매핑은 파일 한 곳(`src/lib/sheets/schema.ts`)에 모아줘.
> - **모바일 우선 반응형**(태블릿/PC 대응), **라이트/다크/시스템 테마**, **글자 크기 4단계 + 폰트 3종 선택**을 설정 화면과 함께 구현해줘.
